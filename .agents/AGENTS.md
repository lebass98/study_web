# Auto Git Commit, Push & Pull Rule

작업이나 요청이 완료되면 항상 다음 절차를 자동으로 실행합니다.
1. `git add .` 명령으로 변경된 모든 파일을 스태이징합니다.
2. 수행한 작업 내용을 한글(한국어)로 작성한 커밋 메시지로 `git commit`을 수행합니다.
3. `git pull`을 실행하여 최신 원격 변경사항을 반영합니다.
4. `git push`를 실행하여 원격 저장소에 최종 반영합니다.
